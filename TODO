Bugs:
    P0:
    P1:
    P2:
        - Perfect poetry indentation - verse numbers cause indentation to not
          line up perfectly. Something like tabs is what we want.
        - \end{document} isn't an elegant way to kill compilation, as it gives
          an error when used within an environment (other than document)
